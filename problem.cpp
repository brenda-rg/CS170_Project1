#ifndef PROBLEM_CPP_
#define PROBLEM_CPP_

#include <iostream>
#include <vector>
#include <queue>
#include <list>
#include <string>
#include "node.h"
#include "problem.h"
using namespace std;

Problem::Problem(){};

vector<int> moveup(vector<int>, int) {
    vector<int> newConfig = {-1};
    cout <<"--FIX func() moveup--" << endl;
    return newConfig;
};

vector<int> movedown(vector<int>, int){
    vector<int> newConfig = {-1};
    cout <<"--FIX func() moveup--" << endl;
    return newConfig;
};

vector<int> moveright(vector<int>, int) {
    vector<int> newConfig = {-1};
    cout <<"--FIX func() moveup--" << endl;
    return newConfig;
};

vector<int> moveleft(vector<int>, int) {
    vector<int> newConfig = {-1};
    cout <<"--FIX func() moveup--" << endl;
    return newConfig;
};

#endif PROBLEM_CPP_